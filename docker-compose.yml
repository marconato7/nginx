services:

  nginx:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: nginx
    hostname: nginx
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs
