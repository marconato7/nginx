# commands

```bash
sudo apt update -y
```

```bash
sudo apt install nginx -y
```

## docker

```bash
docker compose up --build --force-recreate -d
```

```bash
docker run --publish 8080:80 nginx
```

## misc.

```bash
netstat -nltup
```
