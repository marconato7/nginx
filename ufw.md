# ufw

```bash
sudo ufw allow 22/tcp
```

```bash
sudo ufw allow 80/tcp
```

```bash
sudo ufw allow http
```

```bash
sudo ufw allow https
```

```bash
sudo ufw allow ssh
```
```bash
sudo ufw deny ssh
```

```bash
sudo ufw enable
```

```bash
sudo ufw status
```
